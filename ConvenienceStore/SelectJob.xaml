<Page
    x:Class="ConvenienceStore.SelectJob"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ConvenienceStore"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="0.2*"/>
                <RowDefinition Height="0.5*"/>
                <RowDefinition Height="0.3*"/>
            </Grid.RowDefinitions>
            <Grid Grid.Row="1" VerticalAlignment="Stretch" HorizontalAlignment="Center">
                <StackPanel Orientation="Horizontal">
                    <Button Content="구매" x:Name="Purchase" FontSize="40" Width="300" Height="100" Margin="0,0,50,0" Click="Purchase_Click" ></Button>
                    <Button Content="반품" x:Name="Refund" FontSize="40" Width="300" Height="100" Margin="50,0,0,0" Click="Refund_Click" ></Button>
                </StackPanel>
            </Grid>
        </Grid>
    </Grid>
</Page>
